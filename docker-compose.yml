name: walhi-jabar

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: walhi_app
    restart: unless-stopped
    command: ["php", "artisan", "serve", "--host=0.0.0.0", "--port=8000"]
    ports:
      - "8000:8000"
    environment:
      - APP_NAME=WALHI Jabar
      - APP_ENV=local
      - APP_DEBUG=false
      - APP_URL=http://localhost:8000
      - DB_CONNECTION=sqlite
      - DB_DATABASE=/var/www/html/storage/app/database.sqlite
      - SESSION_DRIVER=file
      - CACHE_STORE=file
      - QUEUE_CONNECTION=sync
      - LOG_CHANNEL=stack
      - LOG_LEVEL=debug
      - MIGRATE_ON_START=true
      - SEED_ON_START=true
      - ADMIN_EMAIL=${ADMIN_EMAIL:-admin@walhijabar.or.id}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-}
    volumes:
      # Only persist the SQLite database — NOT the entire storage/ directory
      # This prevents volume shadowing of storage/framework/* dirs built into the image
      - walhi_db:/var/www/html/storage/app
    healthcheck:
      test: ["CMD", "php", "artisan", "about", "--no-ansi"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  walhi_db:
